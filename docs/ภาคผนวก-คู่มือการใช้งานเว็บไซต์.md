## ภาคผนวก A: คู่มือการใช้งานเว็บไซต์

เอกสารฉบับนี้จัดทำขึ้นเพื่อเป็นภาคผนวกประกอบงานวิจัย อธิบายการใช้งานระบบอย่างเป็นขั้นตอน พร้อมแนบโค้ดสำคัญ ขั้นตอนการทดลอง และผลการทดสอบระบบที่เกี่ยวข้อง โดยคงรูปแบบการเขียนตามมาตรฐานงานวิจัยภาษาไทย และระบุชื่อภาพประกอบในรูปแบบ “ภาพที่ A.x …” ให้ครบถ้วน

### A.1 หน้าแรก (Home)
- ภาพที่ A.1: หน้าแรกของระบบ (ไฟล์ภาพตัวอย่าง: `assets/images/photo.png`)
- คำอธิบาย: หน้าแรกแสดงรายการหอพักที่ระบบแนะนำและรายการอัปเดตล่าสุด ผู้ใช้สามารถคลิกเพื่อดูรายละเอียดเชิงลึก เช่น ราคา ช่วงราคา ภาพประกอบ สิ่งอำนวยความสะดวก และตำแหน่งบนแผนที่ เพื่อช่วยในการตัดสินใจได้รวดเร็วยิ่งขึ้น

### A.2 หน้าแผนที่หอพักทั้งหมด (Map)
- ภาพที่ A.2: แผนที่แสดงตำแหน่งหอพักทั้งหมด พร้อมหมายเลขกำกับปุ่ม/ส่วนต่าง ๆ (ไฟล์ภาพตัวอย่าง: `assets/images/photo.png`)
- คำอธิบายแบบกำกับหมายเลข (ผู้จัดทำสามารถวางกรอบสี่เหลี่ยมหมายเลข 1–10 ทับบนภาพ):
  1) ช่องค้นหา/กรองโซน: เลือกโซนเพื่อกรองเฉพาะหอพักในพื้นที่ที่สนใจ และพิมพ์คำค้นเพื่อค้นหาอย่างรวดเร็ว
  2) ตัวกรองช่วงราคา: เลือกช่วงราคาต่ำสุด–สูงสุด ระบบจะคัดเฉพาะหอพักที่อยู่ในช่วงดังกล่าว
  3) การจัดเรียงผลลัพธ์: เรียงตามราคา/ความใหม่ เพื่อให้เห็นหอพักที่เหมาะสมก่อน
  4) ปุ่มสลับแผนที่ถนน/ภาพถ่ายดาวเทียม: สลับสไตล์แผนที่เพื่อดูบริบทพื้นที่จริง (อาคาร/ถนน)
  5) ปุ่มระบุตำแหน่งฉัน (Geolocate): ให้แผนที่เลื่อนไปยังตำแหน่งปัจจุบันของผู้ใช้ (เมื่ออนุญาตสิทธิ์)
  6) หมุดหอพัก: แทนหอพักแต่ละแห่งบนแผนที่ คลิกเพื่อเปิดข้อมูลย่อ
  7) หน้าต่างข้อมูลย่อ (Popup): แสดงชื่อหอพัก ที่อยู่ โซน ช่วงราคา และรูปภาพตัวอย่าง กด “ดูรายละเอียด” เพื่อไปหน้าเฉพาะหอพัก
  8) ป้ายแสดงช่วงราคา: แปลงข้อมูล min–max ของราคาเป็นข้อความอ่านง่าย เช่น “5,000–8,000 บาท/เดือน”
  9) ปุ่มรีเซ็ตตัวกรอง: เคลียร์เงื่อนไขทั้งหมดและแสดงหอพักทั้งหมดอีกครั้ง
  10) ลิงก์นำทางภายนอก: คลิกหมุดหรือปุ่มในหน้าต่างย่อเพื่อเปิด Google Maps ระบุตำแหน่งเส้นทางได้
  หมายเหตุ: แผนที่จะอัปเดตอัตโนมัติเมื่อฐานข้อมูลมีการเพิ่ม/แก้ไขหอพัก และคงตำแหน่ง/ระดับซูมเดิมเมื่อสลับสไตล์แผนที่

### A.3 หน้าค้นหาหอพัก (Search/List)
- ภาพที่ A.3: หน้าค้นหาและตัวกรองหอพัก (ไฟล์ภาพตัวอย่าง: `assets/images/photo.png`)
- คำอธิบาย: ผู้ใช้ค้นหาด้วยชื่อหอพัก โซน ช่วงราคา และจัดเรียงผลลัพธ์ตามราคา/ความใหม่ ระบบรองรับการค้นหา “หอพักที่คล้ายกัน” โดยอาศัยโซน ช่วงราคา และความคล้ายของชื่อหอพัก เพื่อช่วยให้เจอหอพักที่ตรงความต้องการ

### A.4 หน้าล็อกอิน (Login)
- ภาพที่ A.4: หน้าล็อกอิน (ไฟล์ภาพตัวอย่าง: `assets/images/photo.png`)
- คำอธิบาย: รองรับการเข้าสู่ระบบด้วยอีเมล/รหัสผ่านและ Google OAuth แยกประเภทผู้ใช้เป็นสมาชิก (member) และเจ้าของหอพัก (owner) พร้อมฟังก์ชัน “ลืมรหัสผ่าน” ผ่านอีเมลในภาษาไทย

### A.5 หน้าสมัครสมาชิก (Register)
- ภาพที่ A.5: หน้าสมัครสมาชิก (ไฟล์ภาพตัวอย่าง: `assets/images/photo.png`)
- คำอธิบาย: ผู้ใช้ใหม่ลงทะเบียนโดยกรอกอีเมล รหัสผ่าน ชื่อ-สกุล เบอร์โทร และเลือกประเภทผู้ใช้ หากเป็นเจ้าของหอพักให้ระบุข้อมูลการติดต่อเพิ่มเติม ระบบรองรับการสมัครผ่าน Google และการอัปโหลดรูปโปรไฟล์

### A.6 หน้าแก้ไขข้อมูลผู้ใช้ (Profile)
- ภาพที่ A.6: หน้าแก้ไขข้อมูลผู้ใช้ (ไฟล์ภาพตัวอย่าง: `assets/images/photo.png`)
- คำอธิบาย: ผู้ใช้แก้ไขชื่อแสดงผล ชื่อผู้ใช้ เบอร์โทร และรูปโปรไฟล์ได้ ระบบตรวจสอบความถูกต้องก่อนบันทึก และอัปเดตข้อมูลให้เป็นปัจจุบัน

### A.7 หน้าของเจ้าของหอพัก (Owner Dashboard)
- ภาพที่ A.7: รายการหอพักของเจ้าของ (ไฟล์ภาพตัวอย่าง: `assets/images/photo.png`)
- คำอธิบาย: แสดงรายการหอพักที่เจ้าของลงประกาศ สามารถตรวจสอบ/แก้ไข/ลบ และติดตามสถานะการอนุมัติได้โดยตรง

### A.8 หน้าลงประกาศหอพัก (Add Dormitory)
- ภาพที่ A.8: ฟอร์มลงประกาศหอพัก (ไฟล์ภาพตัวอย่าง: `assets/images/photo.png`)
- คำอธิบาย: ฟอร์มบันทึกชื่อหอพัก ราคา สิ่งอำนวยความสะดวก พิกัด และรูปภาพ เมื่อส่งข้อมูล ระบบจะส่งต่อให้ผู้ดูแลตรวจสอบก่อนเผยแพร่

### A.9 หน้าแก้ไขข้อมูลหอพัก (Edit Dormitory)
- ภาพที่ A.9: แก้ไขรายละเอียดหอพัก พร้อมหมายเลขกำกับปุ่ม/ส่วนต่าง ๆ (ไฟล์ภาพตัวอย่าง: `assets/images/photo.png`)

คำอธิบายแบบภาษาคน (อ้างอิงหมายเลขบนภาพ):

การแก้ไขข้อมูลหอพักสามารถทำได้โดยกดปุ่ม (9) เพื่อไปยังหน้า “แก้ไขข้อมูลหอพัก” (1) จากนั้นผู้ใช้สามารถแก้ไขข้อมูลที่ต้องการในส่วนนี้ได้ตามสะดวก หากยังไม่อยากแก้ไขในส่วนข้อมูลพื้นฐาน ก็สามารถกดปุ่ม “ถัดไป” เพื่อไปยังส่วน “สิ่งอำนวยความสะดวก” (2) แล้วปรับรายการให้ตรงกับหอพักของคุณได้ทันที

เมื่อแก้ไขข้อมูลเรียบร้อยแล้ว ให้กดปุ่ม “บันทึกข้อมูล” (3) เพื่อบันทึกการเปลี่ยนแปลง หรือหากต้องการแก้ไขส่วนอื่นเพิ่มเติม เช่น “ข้อมูลช่องทางการติดต่อ” (4)–(5), “ข้อมูลระยะทาง” (6) หรือ “ตำแหน่งของหอพัก” (7) ก็สามารถดำเนินการได้ตามลำดับ และเมื่อแก้ไขเสร็จในแต่ละส่วน อย่าลืมกดปุ่ม “บันทึกข้อมูล” (8) อีกครั้งเพื่อยืนยันการบันทึก

นอกจากนี้ หากเจ้าของหอพักต้องการลบข้อมูลหอพักออกจากระบบ สามารถกดปุ่ม (10) ได้เลย จากนั้นระบบจะแสดง “หน้าต่างยืนยันการลบ” เพื่อความปลอดภัย ให้กดปุ่ม (11) เพื่อยืนยันการลบ เมื่อตกลงแล้ว ระบบจะลบหอพักออกจากฐานข้อมูลและปรับปรุงรายการให้เป็นปัจจุบันโดยอัตโนมัติ

ข้อแนะนำ: เพื่อป้องกันข้อมูลสูญหาย ควรกด “บันทึกข้อมูล” เป็นระยะ โดยเฉพาะก่อนสลับไปแก้ไขส่วนอื่น ๆ เช่น สิ่งอำนวยความสะดวก แผนที่ หรือช่องทางการติดต่อ

### A.10 หน้ารายการสมาชิกหอพัก (Tenant List)
- ภาพที่ A.10: รายการสมาชิกของหอพัก (ไฟล์ภาพตัวอย่าง: `assets/images/photo.png`)
- คำอธิบาย: สำหรับเจ้าของหอพัก ใช้ตรวจสอบคำขอเข้าร่วมเป็นลูกหอ อนุมัติ/ปฏิเสธ รวมถึงยกเลิกสมาชิกในกรณีย้ายหอพัก


## ภาคผนวก B: สถาปัตยกรรมและโครงสร้างระบบ

### B.1 ภาพรวมสถาปัตยกรรม
- ภาพที่ B.1: แผนภาพโครงสร้างระบบ (ไฟล์ภาพตัวอย่าง: `assets/images/logo.png`)
- คำอธิบาย: Frontend (Angular) ติดต่อ Backend API ผ่าน `environment.backendApiUrl` จัดการการยืนยันตัวตนผ่าน Firebase Authentication และเชื่อมต่อแผนที่ด้วย MapTiler SDK การแสดงผลข้อมูลหอพักดึงจาก API หลายชุด เช่น รายการล่าสุด/แนะนำ รายละเอียดภาพ ห้องพัก และข้อมูลแผนที่

### B.2 โฟลว์ข้อมูลสำคัญ
- การยืนยันตัวตน: Firebase → ดึง Token → เรียก Backend `/auth/me` เพื่อโหลดโปรไฟล์ → กระจายสถานะผ่าน `currentUser$`
- แผนที่: Frontend เรียก `/dormitories/map/all` เพื่อรับข้อมูลหมุดและช่วงราคา → MapTiler แสดงผลและสลับสไตล์ถนน/ดาวเทียมได้
- ค้นหา/กรอง: เรียก `/dormitories`, `/dormitories/latest`, `/dormitories/recommended` และกรองฝั่ง Client ตามโซน/ช่วงราคา


## ภาคผนวก C: โค้ดสำคัญของระบบ (อ้างอิงจากซอร์สโค้ด)

### C.1 การเข้าสู่ระบบด้วยอีเมลและการตรวจสอบประเภทผู้ใช้
คำอธิบาย: ป้องกันการเข้าสู่ระบบข้ามบทบาท หากบทบาทจริงไม่ตรงกับที่ผู้ใช้เลือกจะบังคับออกจากระบบทันที

```210:237:src/app/services/auth.service.ts
  async signInWithEmail(
    email: string,
    password: string,
    expectedUserType: 'member' | 'owner'
  ): Promise<UserProfile> {
    try {
      this.authState.skipAuthStateChange = true;
      const userCredential = await signInWithEmailAndPassword(this.auth, email, password);
      const user = userCredential.user;
      const userProfile = await this.fetchUserProfile(user);
      if (userProfile.memberType !== expectedUserType) {
        await this.signOut(null);
        const thaiRole = userProfile.memberType === 'owner' ? 'เจ้าของหอพัก' : 'สมาชิก';
        throw new Error(`บัญชีนี้ถูกลงทะเบียนเป็น${thaiRole}แล้ว`);
      }
      this.currentUser$.next(userProfile);
      return userProfile;
    } finally {
      this.authState.skipAuthStateChange = false;
    }
  }
```

### C.2 ดึงข้อมูลแผนที่สำหรับหมุดหอพักทั้งหมดและแมปเป็นโครงสร้าง UI

```285:318:src/app/services/dormitory.service.ts
  getAllDormitoriesForMap(): Observable<{dormitories: Dorm[], total: number}> {
    return this.http.get<any>(`${this.backendUrl}/dormitories/map/all`).pipe(
      map(response => {
        const dorms = response.dormitories || [];
        const mappedDorms = dorms.map((dorm: any) => ({
          dorm_id: dorm.id,
          dorm_name: dorm.name,
          address: dorm.address,
          latitude: dorm.position?.lat || null,
          longitude: dorm.position?.lng || null,
          zone_name: dorm.zone,
          thumbnail_url: dorm.image_url,
          main_image_url: dorm.image_url,
          min_price: dorm.price_range?.min,
          max_price: dorm.price_range?.max,
          rating: dorm.rating?.average,
          price_display: dorm.price_range ? 
            `${dorm.price_range.min.toLocaleString()} - ${dorm.price_range.max.toLocaleString()} บาท/เดือน` : 
            'ติดต่อสอบถาม'
        }));
        return { dormitories: mappedDorms, total: response.pagination?.total || mappedDorms.length };
      })
    );
  }
```

### C.3 การเริ่มต้นแผนที่และหมุดด้วย MapTiler SDK พร้อมสลับสไตล์ดาวเทียม

```76:99:src/app/services/map.service.ts
    this.map = new maptilersdk.Map({
      container: containerId,
      style: maptilersdk.MapStyle.STREETS,
      center: [lng, lat],
      zoom: 15,
      maxZoom: 20,
      minZoom: 3,
      pitch: 0,
      bearing: 0,
      navigationControl: false,
      geolocateControl: false,
      attributionControl: false
    });
    this.map.on('load', () => {
      this.addControls();
      this.addMarker(lat, lng, dormName, location);
    });
```

```262:287:src/app/services/map.service.ts
  toggleMapStyle(): void {
    if (!this.map) return;
    this.isSatelliteView = !this.isSatelliteView;
    const nextStyle = this.isSatelliteView
      ? `https://api.maptiler.com/maps/hybrid/style.json?key=${maptilersdk.config.apiKey}`
      : maptilersdk.MapStyle.STREETS;
    const center = this.map.getCenter();
    const zoom = this.map.getZoom();
    const pitch = this.map.getPitch();
    const bearing = this.map.getBearing();
    this.map.setStyle(nextStyle);
    this.map.once('style.load', () => {
      this.map?.setCenter(center);
      this.map?.setZoom(zoom);
      this.map?.setPitch(pitch);
      this.map?.setBearing(bearing);
      if (this.marker) {
        const c = this.marker.getLngLat();
        this.addMarker(c.lat, c.lng);
      }
    });
  }
```

### C.4 ฟรอนต์เอนด์: ล็อกอินและการปกปิดสาเหตุข้อผิดพลาดเพื่อความปลอดภัย

```172:199:src/app/main/login/login.component.ts
  async onSubmit(): Promise<void> {
    this.isLoginLoading = true;
    this.errorMessage = null;
    try {
      const formValue = this.loginForm.getRawValue();
      const userProfile = await this.authService.signInWithEmail(formValue.email, formValue.password, this.userType);
      if (userProfile && userProfile.provider === 'google') {
        this.errorMessage = 'บัญชีนี้สมัครผ่าน Google กรุณาใช้ Connect with Google';
        await this.authService.signOut(null);
        return;
      }
      if (userProfile) {
        await new Promise(resolve => setTimeout(resolve, 100));
        this.redirectBasedOnUserType(userProfile);
      }
    } catch (error: any) {
      this.errorMessage = this.getGenericLoginError(error);
    } finally {
      this.isLoginLoading = false;
    }
  }
```

### C.5 ฟรอนต์เอนด์: สมัครสมาชิก (รองรับ Normal และ Google OAuth)

```1031:1094:src/app/main/register/register.component.ts
    try {
      const formData = this.registerForm.getRawValue();
      const currentUserType = this.userType;
      const cleanedFormData = { ...formData, email: formData.email?.toString().trim(), fullName: formData.fullName?.toString().trim(), phoneNumber: formData.phoneNumber?.toString().trim().replace(/\D/g, '') };
      let userProfile: UserProfile;
      if (this.isFromGoogle) {
        const ownerData = currentUserType === 'owner' ? { managerName: cleanedFormData.managerName, secondaryPhone: cleanedFormData.secondaryPhone || undefined, lineId: cleanedFormData.lineId || undefined } : undefined;
        userProfile = await this.registerService.completeGoogleUserProfile(
          cleanedFormData.phoneNumber || undefined,
          currentUserType as 'member' | 'owner',
          currentUserType === 'member' && cleanedFormData.dormitory ? parseInt(cleanedFormData.dormitory, 10) : undefined,
          ownerData
        );
      } else {
        const submitFormData = new FormData();
        submitFormData.append('email', cleanedFormData.email);
        submitFormData.append('password', cleanedFormData.password);
        submitFormData.append('memberType', currentUserType);
        submitFormData.append('fullName', cleanedFormData.fullName);
        if (cleanedFormData.phoneNumber) submitFormData.append('phoneNumber', cleanedFormData.phoneNumber);
        if (currentUserType === 'member' && cleanedFormData.dormitory) submitFormData.append('dormitoryId', cleanedFormData.dormitory);
        if (this.selectedFile) submitFormData.append('profileImage', this.selectedFile);
        if (currentUserType === 'owner') {
          submitFormData.append('managerName', cleanedFormData.managerName);
          if (cleanedFormData.secondaryPhone) submitFormData.append('secondaryPhone', cleanedFormData.secondaryPhone);
          if (cleanedFormData.lineId) submitFormData.append('lineId', cleanedFormData.lineId);
        }
        userProfile = await this.registerService.signUpWithFormData(submitFormData);
      }
```


## ภาคผนวก D: ขั้นตอนการทดลองและการทดสอบระบบ

### D.1 ขั้นตอนการติดตั้งและตั้งค่าโดยสรุป
1) ติดตั้ง Node.js และตั้งค่า `environment.ts` ให้ชี้ `backendApiUrl` และคีย์ MapTiler/Firebase ให้ถูกต้อง 2) ติดตั้งแพ็กเกจ `npm install` 3) รันระบบ `npm start` และตรวจสอบการเชื่อมต่อกับ Backend และ Firebase

### D.2 แผนการทดสอบเชิงหน้าที่ (Functional Test Cases)
- การยืนยันตัวตน: สมัครสมาชิก, ล็อกอิน, ลืมรหัสผ่าน, ออกจากระบบ, ป้องกันข้ามบทบาท
- การจัดการหอพัก: เพิ่ม/แก้ไข/ลบ, อัปโหลดภาพ, ตรวจสอบราคาและโซน
- การค้นหาและแผนที่: กรองตามโซน/ราคา, คลิกหมุดเพื่อดูข้อมูลย่อ, เปิดนำทาง Google Maps
- ความเสถียร UI: สไลด์โชว์หน้า Login/Register, การกู้คืนสถานะ URL, การปรับขนาดแผนที่/สลับสไตล์

ตัวอย่างตารางทดสอบ:

| หมายเลข | กรณีทดสอบ | ขั้นตอน | ผลลัพธ์ที่คาดหวัง |
|---|---|---|---|
| TC-Auth-01 | Login ด้วยอีเมลถูกต้อง | กรอกอีเมล/รหัสผ่าน กดเข้าสู่ระบบ | เข้าระบบและถูกส่งไปยัง `/main` หรือ `/owner` ตามบทบาท |
| TC-Auth-02 | ป้องกันข้ามบทบาท | เลือก Owner แต่บัญชีเป็น Member | แจ้งเตือน “บัญชีนี้ถูกลงทะเบียนเป็นสมาชิกแล้ว” และ Sign-out |
| TC-Map-01 | แสดงหมุดทั้งหมด | เปิดหน้าแผนที่ | เห็นหมุดตามจำนวนจาก API และคลิกเพื่อเปิดข้อมูลย่อได้ |
| TC-Search-01 | ค้นหาตามโซน | เลือกโซนและช่วงราคา | รายการถูกกรองตรงตามเงื่อนไข |

### D.3 ตัวอย่างผลการทดสอบเชิงประสิทธิภาพ (เชิงคุณภาพ)
- โหลดรายการแนะนำ/ล่าสุดเฉลี่ย < 2 วินาที (เครือข่ายปกติ) และแผนที่เริ่มทำงานภายใน ~1–2 วินาทีหลังสไตล์โหลด
- สลับสไตล์แผนที่ถนน/ดาวเทียม ทำได้ต่อเนื่องโดยคงตำแหน่ง/ซูมเดิม


## ภาคผนวก E: ผลการทดสอบโมดูล AI/ตรรกะอัตโนมัติที่เกี่ยวข้อง

ระบบรีวิวหอพักมีการส่งข้อความความคิดเห็นไปยัง Backend ซึ่งอาจมีโมดูลประเมินคะแนนอัตโนมัติ (auto-rating) ทำให้ไม่ต้องให้ผู้ใช้ให้คะแนนเองในขั้นต้น ฝั่ง Frontend เรียกใช้งานผ่านบริการต่อไปนี้

ตัวอย่างการเรียกใช้งานสร้างรีวิว:

```json
POST /reviews/dormitory/{dormId}
{
  "comment": "หอพักสะอาด เดินทางสะดวก ใกล้ร้านอาหาร"
}
```

ตัวอย่างผลลัพธ์ที่คาดหวังจาก Backend:

```json
{
  "reviewId": 123,
  "dormId": 45,
  "autoRating": 4.5,
  "sentiment": "positive",
  "message": "รีวิวถูกบันทึกสำเร็จ"
}
```

หมายเหตุ: คะแนนและชนิดอารมณ์เป็นผลจากโมดูล AI/กติกาธุรกิจในฝั่ง Backend ซึ่งอยู่นอกขอบเขตโค้ดฝั่ง Frontend ในที่เก็บนี้ การทดสอบฝั่งผู้ใช้จึงมุ่งเน้นความถูกต้องของการเรียก API การแสดงผล และการควบคุมสิทธิ์


## ภาคผนวก F: ตารางข้อมูลตัวอย่างสำหรับงานวิจัย

### F.1 โครงสร้างข้อมูลหอพักที่ใช้แสดงผล (สรุป)

| ฟิลด์ | คำอธิบาย |
|---|---|
| dorm_id | รหัสหอพัก |
| dorm_name | ชื่อหอพัก |
| zone_name | ชื่อโซนที่ตั้ง |
| latitude, longitude | พิกัดตำแหน่ง |
| min_price, max_price | ช่วงราคา/เดือน |
| rating | คะแนนเฉลี่ยจากรีวิว |
| main_image_url | รูปภาพหลักของหอพัก |

### F.2 ตัวอย่างข้อมูลย่อเพื่อทดสอบแผนที่ (สมมติ)

| dorm_id | dorm_name | zone_name | lat | lng | min_price | max_price |
|---|---|---|---|---|---|---|
| 1 | A-Dorm | กลางเมือง | 13.736 | 100.523 | 4500 | 6500 |
| 2 | B-Place | ใกล้มหาวิทยาลัย | 13.750 | 100.540 | 5000 | 8000 |


## ภาคผนวก G: คำแนะนำการใช้งานและการบำรุงรักษา

- อัปเดตค่า `environment.ts` ให้ถูกต้องเมื่อเปลี่ยนโดเมน Backend หรือคีย์บริการภายนอก
- ติดตามสถานะผู้ใช้ผ่าน `AuthService.currentUser$` เพื่อป้องกันปัญหา redirect หรือสิทธิ์ไม่ตรงบทบาท
- เมื่อพบปัญหาแผนที่ไม่แสดง ให้ตรวจดู WebGL context และคีย์ MapTiler (ดู `MapService`) รวมถึงสิทธิ์การโหลดรูปภาพจากโดเมนภายนอก


## รายการภาพประกอบ
- ภาพที่ A.1 หน้าแรกของระบบ — `assets/images/photo.png`
- ภาพที่ A.2 แผนที่หอพักทั้งหมด — `assets/images/photo.png`
- ภาพที่ A.3 หน้าค้นหาหอพัก — `assets/images/photo.png`
- ภาพที่ A.4 หน้าล็อกอิน — `assets/images/photo.png`
- ภาพที่ A.5 หน้าสมัครสมาชิก — `assets/images/photo.png`
- ภาพที่ A.6 หน้าแก้ไขข้อมูลผู้ใช้ — `assets/images/photo.png`
- ภาพที่ A.7 รายการหอพักของเจ้าของ — `assets/images/photo.png`
- ภาพที่ A.8 หน้าลงประกาศหอพัก — `assets/images/photo.png`
- ภาพที่ A.9 หน้าแก้ไขข้อมูลหอพัก — `assets/images/photo.png`
- ภาพที่ A.10 หน้ารายการสมาชิกหอพัก — `assets/images/photo.png`
- ภาพที่ B.1 แผนภาพโครงสร้างระบบ — `assets/images/logo.png`


หมายเหตุการใช้งานไฟล์ภาพ: สามารถแทนที่ไฟล์ภาพตัวอย่างด้วยสกรีนช็อตจริงจากระบบ โดยเก็บไว้ในโฟลเดอร์ `src/assets/images/` และปรับปรุงชื่อไฟล์ในรายการภาพประกอบให้สอดคล้อง


